# apps/web

Target home for the production Next.js web client.

Current repo UI still runs from the legacy `src/` Vite app. This folder is introduced to enable phased migration into a monorepo layout.
