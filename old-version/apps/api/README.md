# apps/api

Target home for the production API service (NestJS/Express TypeScript).

Current API still runs from `backend/`. This folder is introduced so CI/CD and infra can evolve without breaking the existing service.
